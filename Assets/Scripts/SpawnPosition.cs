using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPosition : MonoBehaviour {

	SpawnPosition (DepthMatrix depthMatrix, Layer layer)
	{
		// Select a random point within the desired layer and 
	}
}
